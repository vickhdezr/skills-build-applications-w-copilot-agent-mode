OctoFit Tracker — Backend (Express + TypeScript)

Run development server:

```bash
npm --prefix . run dev
```

Environment:
- PORT (default 8000)
- MONGO_URI (default mongodb://localhost:27017/octofit)

Health check: `GET /api/health`
